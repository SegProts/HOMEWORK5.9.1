#include "Snack.h"

Snack::Snack(string nameSnackPtr, double priceSnakePtr, int curentCountSnakePtr, int indexProductSnakePtr) // реализация конструктора для класса snack
{
	Snack::nameSnack = nameSnackPtr;
	Snack::priceSnake = priceSnakePtr;
	Snack::curentCountSnake = curentCountSnakePtr;
	Snack::indexProductSnake = indexProductSnakePtr;
}